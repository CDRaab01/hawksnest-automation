# Staging dummy credentials — NOT real. Ring never logs in on staging (it boots
# best-effort and is not asserted Ready), so this placeholder is fine. Key name
# matches prod (RING_MQTT_PASSWORD) so the env wiring is identical.
RING_MQTT_PASSWORD=staging-ring-pw
