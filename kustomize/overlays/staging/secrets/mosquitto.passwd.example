# Staging dummy mosquitto password file — intentionally has no users.
# Mosquitto runs with `allow_anonymous false`, so with an empty user list nothing
# can authenticate — which is fine for a smoke test: the broker still starts and
# the pod reaches Ready, and ring-mqtt (best-effort on staging) simply can't log in.
# A real hashed entry isn't generated here because it would require mosquitto_passwd.
