# Real secrets — never commit. Only the *.example templates are tracked.
# Each overlay (overlays/prod, overlays/staging) has its own secrets/ dir.
kustomize/overlays/*/secrets/*
!kustomize/overlays/*/secrets/*.example
