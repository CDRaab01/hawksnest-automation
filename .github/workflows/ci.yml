name: CI

# Static validation of the manifests — runs on GitHub-hosted runners (no cluster
# needed). This is intentionally separate from deploy.yml (which runs on the
# self-hosted runner inside Dragonfly and applies to live door locks).
on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Invariant checks (PVC/Secret wiring, NFS v3, device path, ports)
        run: |
          pip install pyyaml
          python3 tests/validate_manifests.py

      - name: Install kustomize + kubeconform
        run: |
          curl -sSfL https://raw.githubusercontent.com/kubernetes-sigs/kustomize/master/hack/install_kustomize.sh | bash
          sudo mv kustomize /usr/local/bin/
          curl -sSfL https://github.com/yannh/kubeconform/releases/latest/download/kubeconform-linux-amd64.tar.gz \
            | tar xz kubeconform
          sudo mv kubeconform /usr/local/bin/

      - name: Stage placeholder secrets so kustomize can build
        # secretGenerator reads gitignored files that never exist in CI. The
        # *.example templates are enough to make `kustomize build` resolve — the
        # real secrets are bootstrapped on the host (see DEPLOYMENT.md §10).
        run: |
          cp kustomize/secrets/mariadb.env.example     kustomize/secrets/mariadb.env
          cp kustomize/secrets/mosquitto.passwd.example kustomize/secrets/mosquitto.passwd
          cp kustomize/secrets/ring-mqtt.env.example   kustomize/secrets/ring-mqtt.env

      - name: Build manifests
        run: kustomize build kustomize/ | tee /tmp/hawksnest.yaml

      - name: Validate against Kubernetes schemas
        run: kubeconform -strict -summary -ignore-missing-schemas /tmp/hawksnest.yaml
